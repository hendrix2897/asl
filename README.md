# asl
Apple Subsystem for Linux
This implies Apple Silicon with swift and apple container installed (https://github.com/apple/container).
Build steps:
- git clone this repository
- swift build -c release
- sudo cp .build/release/asl /usr/local/bin/asl
- asl ubuntu


This is not affiliated with Apple in any shape or form.
